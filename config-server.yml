spring:
  profiles:
   active: cloud
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sanjeeb-talukdar/microservices-demo-config-repo.git
 #       native:
 #         search-locations: file:///D:/Projects/MayBank/microservices-complete-example/sample-config
  application:
    name: config-server    
  
info:
  component: String Cloud Config Server
  
endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false

server:
  port: 80

#management:
#  port: 8995

eureka:
  awsAccessId: AKIAIXPIFMO634VRHDWA
  awsSecretKey: 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
  preferSameZone: true
  shouldUseDns: false
  decoderName: JacksonJsonMini
  serviceUrl:
    default: http://ec2-50-16-33-110.compute-1.amazonaws.com/eureka
  client:
    registerWithEureka: true
    serviceUrl:
      us-east-1a: http://ec2-50-16-33-110.compute-1.amazonaws.com/eureka
  us-east-1:
      availabilityZones: us-east-1a
  instance:
    leaseRenewalIntervalInSeconds: 10
    statusPageUrlPath: /info
    healthCheckUrlPath: /health
    metadataMap:
      instanceId: ${spring.cloud.client.hostname}:${spring.application.name}:${spring.application.instance_id:${server.port}}}
      
logging:
  level:
    ROOT: 'ERROR'
    org.springframework.web: 'ERROR'
    org.springframework.cloud: 'ERROR'
    org.springframework.boot: 'ERROR'
    org.springframework: 'ERROR'
 
  
      